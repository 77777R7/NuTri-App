// Web stub to prevent bundling a native-only dependency.
// The calling code guards by `Platform.OS`, so these should never be used.
export const appleAuth = null;
export const appleAuthAndroid = null;

