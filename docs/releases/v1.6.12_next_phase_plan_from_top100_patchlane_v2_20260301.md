# v1.6.12 下一阶段规划（基于 Top100 Brand PatchLane v2 + 阶段A完成态）

## 1) JSON 内容解读（重点）

输入文件：`/Users/howard07/Downloads/NuTri_Top100_Brand_PatchLane_Plan_v2.json`

### 1.1 文档定位
- 类型：`nutri_patch_lane_priority_plan`
- 版本：`2026-03-01.v2`
- 核心目的：给工程团队一套“Top100 品牌优先 patch lane”执行框架。
- 明确边界：
  - 不做 price/value。
  - 不全库修复，先打热门品牌 Top N。
  - AI 只做抽取/标准化/压缩文案，不允许发明事实。

### 1.2 品牌优先级结构
- US：50 品牌，建议 MVP Top N=20。
- CA：50 品牌，建议 MVP Top N=20。
- 每个品牌包含：`rank`、`patch_priority_score`、`why_prioritize`、`first_patch_lanes`、`mvp_scope_rule`。
- 推荐合并 MVP 范围（JSON 已给）：US Top15 + CA Top15。

### 1.3 阶段计划（B-E）
- Stage B：decision layer 收口（让 verdict 真可决策，不再解释系统）。
- Stage C：Patch Lane MVP（两条 ROI 最高 lane：DSLD directions + fish oil EPA/DHA）。
- Stage D：fixable vs ceiling 运营化（队列闭环、allowlist TTL、持续 reconcile）。
- Stage E：品类扩展（Vitamin D form -> Magnesium -> Probiotics）。

### 1.4 Patch 规则模板（可直接映射实现）
- 全局规则：
  - 只做 overlay patch，不覆盖 raw LNHPD/DSLD。
  - 每条 patch 必带 `identityKey/fieldKey/patchedValue/sourceTier/evidenceRef/confidence/owner/status/expiresAt/reasonCode`。
  - `mvp_allowed_source_tiers`: `official_record`, `scanned_label`。
- 具体规则：
  - `patch_directions_text_v1`
  - `patch_fish_oil_breakdown_v1`
  - `patch_vitamin_d_form_v1`
  - `patch_magnesium_form_elemental_v1`
  - `patch_probiotics_strain_cfu_v1`

### 1.5 DB 覆盖统计模板
- 目标输出：
  - `brand_scope_products`
  - `brand_coverage_summary`
  - `brand_missing_field_distribution`
  - `brand_category_patch_candidates`
- 先导入 `target_brands_seed`，再跑 SQL 模板。

## 2) 当前状态基线（阶段A已完成）

以最新 full stable（no skip）为准：
- 目录：`/Users/howard07/NuTriApp/nutri-app/output/v1.6.12-r2d-full-stable-phaseA-final-20260301T091230Z`
- 关键结果：
  - `systemHealthVerdict=pass`
  - `evidenceSufficiencyVerdict=sufficient`
  - `authoritativeExpectedButNotFinalCount=0`
  - `webFallbackCount=0`（`00649908268756` 已归类 `expected_web_only` info）
  - `residualHitRate=0`
  - crash canary：`postCanaryDoneRate=1`, `canaryTerminalRate=1`
- decision-support 观测：
  - `sampleCount=20`
  - `stableDigestUnexpected409Rate=0`
  - `forced409RetrySuccessRate=1`
  - `inlineFallbackProxyRate=0`

结论：阶段A完成，下一阶段可以直接进入 Stage B/C 主线，不需要再补 A 阶段阻断项。

## 3) 下一阶段总目标（建议）

把当前“稳定可用”升级为“可持续变好”的产品化闭环：
1. Stage B：把决策层语义收口到可发布质量（UI/summary/verdict 一致）。
2. Stage C：启动 TopN Patch Lane MVP，先打两条高ROI lane。
3. Stage D：把 fixable/ceiling 队列变成固定运营机制。

## 4) Stage B（Decision Layer 收口）执行计划

## B0. 冻结基线
- 固定当前 artifact 目录与 DoD 文档。
- 以该基线为回归对照，不再改动 A 阶段定义。

## B1. 决策层语义合约验证（真实样本）
- 用 Top100 样本抽样回放验证：
  - 首屏三行：`Best for / Main concern / Before you buy`。
  - blocker 分层：
    - `fixable` -> core blockers
    - `ceiling` -> before-you-buy 提示
  - summary 三句契约：`作用 + 本品提供 + 最大限制`。

## B2. Stage B 验收门禁
- `verdict` 分布不应无差别堆在 `reasonable_but_incomplete`。
- Simple 模式继续保持：
  - 无 inferred 泄漏
  - 无 ratio 文案
  - 无重复 CTA
- 语义禁词检查全绿（无医疗暗示）。

## 5) Stage C（Patch Lane MVP）执行计划

## C0. 建 MVP 范围
- 不做全库。
- 推荐直接采纳 JSON 的合并范围：US Top15 + CA Top15（30 品牌）作为首批。

## C1. seed + coverage 统计
- 导入 `target_brands_seed`（market/brand/rank/score）。
- 实现并跑 JSON 提供的 SQL 模板（映射到当前 schema）。
- 产出四类统计输出（brand_scope/coverage/missing_distribution/candidates）。

## C2. 只上两条 patch lane
1. DSLD `directions_text`（缺失但 label 可抽取）
2. fish oil `EPA/DHA/total_omega3` breakdown（缺失但 label 可抽取）

## C3. Patch 数据层（overlay）
- 新增 patch overlay 表与冲突队列（不改 raw 表）。
- 字段遵循 JSON `required_fields_per_patch`。
- 强制写入规则：
  - 证据不足不写
  - 与 official_record 冲突不覆盖，入 conflict queue

## C4. Shadow -> Enforce
- `shadow apply` 先跑（最少一个完整 watch 窗口）。
- 看冲突率与误伤率。
- 仅对高置信、无冲突 patch 转 enforce。

## C5. Stage C 验收门禁
- TopN 品牌样本中：
  - `missing_directions_dsld` 显著下降
  - `missing_active_breakdown` 显著下降
- 冲突写入接近 0。
- 不引入新的 `authoritativeExpectedButNotFinal`。

## 6) Stage D（运营化闭环）执行计划

## D1. 队列治理字段标准化
- 所有 warning 必有去向：
  - `fixable` -> repair queue
  - `ceiling` -> explain queue
- 队列字段统一：`owner/status/targetRelease/evidenceRef/fixLane`。

## D2. allowlist TTL 机制
- `web-only` 必须 `reviewAfterDays + expiresAt`。
- 过期自动回 triage，不允许永久豁免。

## D3. 周期化动作（无时间绑定）
- replay/triage -> 修复 -> shadow/enforce -> reconcile。

## 7) 你现在就可以执行的“下一步清单”

1. 把 Top100 seed 导入 staging（先 30 品牌 MVP）。
2. 跑 brand coverage SQL 模板，拿第一版缺失分布。
3. 启动 C lane candidate builder（仅两条 lane）。
4. 上 shadow apply + conflict filter。
5. 跑固定序列回归并对照阶段A基线。
6. 通过后再推进 enforce。

## 8) 风险控制（必须遵守）

- 不允许绕过治理链路直接写 DB：
  - migration -> shadow -> gate -> enforce。
- 不允许 AI 生成无证据字段。
- 不允许 patch 覆盖 official_record 明确值。
- 不允许把 quality marks/ceiling 提示当成核心 blocker 拉低 verdict。

## 9) 建议的“阶段完成定义”

Stage B 完成：
- 决策层语义、UI 合约、summary 合约在 Top100 抽样回放全部通过。

Stage C 完成：
- 两条 lane 在 TopN 品牌样本上带来可测提升（directions 与 active breakdown 缺口下降），且无治理回退。

Stage D 完成：
- warning 全量可追踪、无悬空，allowlist 全部具备 TTL 与复核。
