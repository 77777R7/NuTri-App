# Queued/High-Frequency Matcher Result

Generated: 2026-03-17

No discovery-agent result files were present in `docs/exec-plans/active/p0_p3_product_closure/subagents/runs/` while this pass was completed, so the matrix is grounded in the current canonical high-frequency reports plus existing `output/**` brand artifacts only.

## Best Evidence-Backed Matches

- `Pure Encapsulations`: clear active-queue match. March 17 official fallback already processed 38 rows and improved 26, so the current best lane is simply to continue that active-queue wave.
- `Schiff`: clear active-queue match. March 17 official fallback improved 5 of 46 rows; that is weaker than Pure but still better than the zero-match exact-barcode lane.
- `Sports Research`: clear missing-from-staging match with strong local recovery evidence. Existing official queue artifacts, a successful sampled fallback run, and a 15-row quality-marks final all point to a targeted official/iGen replay, not new discovery.
- `Garden of Life`: clear missing-from-staging match. Existing official queue depth plus successful fallback and quality-marks probe outputs make this a good local replay candidate.
- `Natrol`: clear missing-from-staging blocker. Both March 17 Natrol recovery reports processed 41 rows with zero recovery, so this is the strongest case for opening a new external discovery lane.

## Strong But Partly Inferred Matches

- `Nutricost`: active-queue match is strong from the canonical rollup and older official queues, and program-level status explicitly names it as a next-best active brand, but there is no fresh brand-specific March 17 run yet.
- `Vital Proteins`: active-queue match is strong enough to keep in the official-fallback lane because local brand outputs improved 1 of 1 rows and exact-barcode evidence was zero, but the direct proof sample is still small.
- `Life Extension`: missing-from-staging match is present, but only older RapidAPI queue evidence was found. The best next move is to regenerate a current queue from that local signal before escalating to new discovery.
- `Country Life`: missing-from-staging match is present. Exact-barcode evidence is negative, but the March 15 quality-marks probe produced 15 consumer-ready rows, so replaying that local lane is stronger than starting with fresh discovery.

## Brand Requiring Verification First

- `Codeage`: absent from the current canonical high-frequency report. Older RapidAPI queue artifacts exist, but there is no current canonical match or fresh recovery run, so the first step should be fresh North America discovery plus queue-presence verification.
